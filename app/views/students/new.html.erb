<%# render 'form' %>

<%= render partial: "form", locals: {student: @student} %>

<%# locals: {student: @student} 
locals pointing to values that will be assigned to them as local variables within the partial. %>